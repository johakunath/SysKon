# Agent guide (SysKon)

**`CLAUDE.md` is the single source of truth for agent instructions in this repo.**

Read `CLAUDE.md` (and the docs it links: `BACKLOG.md`, `docs/PRODUCT_ROADMAP.md`,
`docs/BACKLOG_WORK_PACKAGES.md`, `docs/BACKLOG_ARCHIVE.md`). This file intentionally
only points to `CLAUDE.md` so the workflow, file map and hard rules live in exactly
one place and cannot drift.
